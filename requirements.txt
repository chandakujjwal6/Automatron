# Core Framework
openenv

# Data validation and typed models (Required by OpenEnv spec)
pydantic>=2.0.0

# For baseline LLM agent
openai

# Optional: For actual SAST tools if we want real scans
# bandit
# semgrep