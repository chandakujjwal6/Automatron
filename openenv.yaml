name: "DevSecOpsEnv"
description: "A simulated DevSecOps environment where an agent fixes security vulnerabilities in codebases."
version: "1.0.0"
entrypoint: "cyber_env.env:DevSecOpsEnv"
tasks:
  - name: "task_1"
    description: "Fix a hardcoded API key in Python code."
  - name: "task_2"
    description: "Prevent command injection in Python subprocess calls."
  - name: "task_3"
    description: "Fix buffer overflow in C++ code."